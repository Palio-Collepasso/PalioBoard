# Raw Documentation Sources

This directory keeps original long-form source material.

Use `_raw` files as source references when syncing or splitting content into the maintained documentation tree under `docs/`.

Rules for this directory:
- keep the original long documents here
- do not treat `_raw` as the canonical surface for routine doc review or cleanup
- do not split or reorganize these files as part of normal documentation maintenance unless the source material itself must change
