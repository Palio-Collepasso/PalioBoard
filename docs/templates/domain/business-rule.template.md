### `BR-XXX` — `<Rule title>`

- **Status:** `<active | deprecated | draft>`
- **Area:** `<season setup | event operations | results | tournaments | live games | leaderboard | auth | public read | other>`
- **Priority:** `<critical | high | normal>`
- **Summary:** `<one-sentence statement of the rule>`

#### Rule statement

`<Write the rule as a clear normative statement. Prefer "must", "must not", "can only", "cannot".>`

#### Applies when

- `<context / trigger>`
- `<context / trigger>`

#### Preconditions

- `<condition>`
- `<condition>`

#### Required behavior

- `<behavior>`
- `<behavior>`

#### Forbidden behavior

- `<behavior that must not happen>`
- `<behavior that must not happen>`

#### Inputs / factors

- `<input or factor>`
- `<input or factor>`

#### Outcome

- `<expected result>`
- `<expected result>`

#### Edge cases

- `<edge case>`
- `<edge case>`

#### Rationale

`<Why this rule exists from a business/domain point of view.>`

#### Examples

- **Valid example:** `<example>`
- **Invalid example:** `<example>`

#### Enforcement notes

- **API impact:** `<relevant API behavior or error codes>`
- **UI impact:** `<relevant UI/admin behavior>`
- **Audit impact:** `<what should be traceable>`
- **Test impact:** `<what must be tested>`

#### Related items

- **Related rules:** `<BR-...>`
- **Related endpoints:** `<method/path>`
- **Related docs:** `<doc path>`
- **Related ADRs:** `<ADR>`
