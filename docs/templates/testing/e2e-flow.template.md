### `E2E-XXX` — `<Flow title>`

- **Status:** `<active | deprecated | draft>`
- **Priority:** `<critical | high>`
- **Cadence:** `<per PR | release | nightly | manual>`
- **Owner:** `<team>`
- **Business value protected:** `<why this flow matters>`

#### Preconditions

- `<environment state>`
- `<seed data / fixture>`
- `<required user role/capability>`

#### Test data / fixtures

- `<fixture name>`
- `<seed assumptions>`

#### Steps

1. `<step>`
2. `<step>`
3. `<step>`

#### Expected result

- `<observable result>`
- `<observable result>`

#### Failure impact

- `<what breaks if this fails>`
- `<who is affected>`

#### Related business rules

- `<BR-...>`
- `<BR-...>`

#### Related API / modules

- `<endpoint>`
- `<module>`

#### Notes / edge cases

- `<edge case>`
- `<known fragility>`

#### Automation status

- **Automated:** `<yes/no/partial>`
- **Test location:** `<path or suite>`
- **Manual fallback:** `<how to verify if automation is unavailable>`
