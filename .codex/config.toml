approval_policy = "on-request"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = false
writable_roots = ["~/codex-worktrees/palio-board"]

[features]
multi_agent = true

[mcp_servers.backlog]
command = "backlog"
args = ["mcp", "start"]