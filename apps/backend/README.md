# Backend

This directory is the canonical home for the FastAPI application and backend test harness.

Planned ownership for later M0 tasks:
- `pyproject.toml` and Python tooling configuration
- Alembic configuration and migrations
- `src/palio/` with the modular-monolith backend packages
- backend unit and integration tests

TASK-1 creates only the top-level layout. TASK-2 and later tasks will populate this directory.
