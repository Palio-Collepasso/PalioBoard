# Web

This directory is the canonical home for the Angular SPA and frontend test harness.

Planned ownership for later M0 tasks:
- `package.json` and npm scripts
- `src/app/` with the admin, public, and maxi-screen shells
- frontend unit tests and Playwright coverage

TASK-1 creates only the top-level layout. TASK-3 and later tasks will populate this directory.
