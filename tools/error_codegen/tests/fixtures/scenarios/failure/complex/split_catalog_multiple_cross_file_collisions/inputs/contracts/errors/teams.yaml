module: teams
errors:
  STATE_CONFLICT:
    code: STATE_CONFLICT
    type_slug: team-state-conflict
    http_status: 409
    title: Team state conflict
    translation_key: errors.teamStateConflict
    context_schema:
      type: object
      additionalProperties: false
      properties:
        team_id: { type: string, format: uuid }
      required: [team_id]
    example_context:
      team_id: "not-a-uuid"
